diff --git a/app.py b/app.py
index 147a234..d3abe87 100644
--- a/app.py
+++ b/app.py
@@ -871,16 +871,24 @@ def add_product():
 @app.route("/products/<int:product_id>/recipe/add", methods=["POST"])
 @login_required
 def add_recipe_ingredient(product_id):
+    """Records how much of one material a single unit of this product uses
+    — e.g. "Salt: 0.3g per unit" — entered directly rather than as a batch
+    that then gets divided by a yield count. Stored in the same batch_qty /
+    yield_count / quantity_per_unit columns for compatibility (batch_qty is
+    just set equal to the per-unit amount, with yield_count fixed at 1), so
+    no schema change is needed and older recipes entered via batch math
+    still display and edit correctly (quantity_per_unit is still the true
+    per-unit figure either way)."""
     db = get_db()
     material_id = request.form.get("material_id")
-    batch_qty = float(request.form.get("batch_qty") or 0)
-    yield_count = float(request.form.get("yield_count") or 0)
+    quantity_per_unit = float(request.form.get("quantity_per_unit") or 0)
 
-    if not material_id or batch_qty <= 0 or yield_count <= 0:
+    if not material_id or quantity_per_unit <= 0:
         flash(g.t("flash_recipe_invalid"), "error")
         return redirect(url_for("products"))
 
-    quantity_per_unit = batch_qty / yield_count
+    batch_qty = quantity_per_unit
+    yield_count = 1
 
     existing = db.execute(
         "SELECT id FROM product_ingredients WHERE product_id = ? AND material_id = ?",
diff --git a/templates/products.html b/templates/products.html
index d840c80..c247167 100644
--- a/templates/products.html
+++ b/templates/products.html
@@ -100,36 +100,22 @@
     chevron.classList.toggle('open', !isOpen);
   }
 
-  function fillRecipeForm(productId, materialId, batchQty, yieldCount) {
+  function fillRecipeForm(productId, materialId, quantityPerUnit) {
     document.getElementById('recipe-material-' + productId).value = materialId;
-    document.getElementById('recipe-batch-' + productId).value = batchQty;
-    document.getElementById('recipe-yield-' + productId).value = yieldCount;
+    document.getElementById('recipe-amount-' + productId).value = quantityPerUnit;
     updateRecipeRow(productId);
   }
 
-  // Keeps the unit label ("kg", "pcs"...) in sync with whichever material is
-  // selected, and shows a live "≈ X per piece" preview so the shop owner can
-  // sanity-check the math before submitting instead of guessing blind.
+  // Keeps the unit label ("kg", "g"...) in sync with whichever material is
+  // selected, so the amount she types is unambiguous — e.g. "0.3" next to
+  // "g" instead of a bare number.
   function updateRecipeRow(productId) {
     const materialSelect = document.getElementById('recipe-material-' + productId);
-    const batchInput = document.getElementById('recipe-batch-' + productId);
-    const yieldInput = document.getElementById('recipe-yield-' + productId);
     const unitLabel = document.getElementById('recipe-unit-' + productId);
-    const preview = document.getElementById('recipe-preview-' + productId);
 
     const selectedOption = materialSelect.options[materialSelect.selectedIndex];
     const unit = selectedOption ? (selectedOption.dataset.unit || '') : '';
     unitLabel.textContent = unit || '{{ t("unit_choose") }}';
-
-    const qty = parseFloat(batchInput.value);
-    const yieldN = parseFloat(yieldInput.value);
-    if (unit && qty > 0 && yieldN > 0) {
-      preview.textContent = '≈ ' + (qty / yieldN).toFixed(4) + unit + '{{ t("per_unit_suffix") }}';
-      preview.classList.add('has-value');
-    } else {
-      preview.textContent = '';
-      preview.classList.remove('has-value');
-    }
   }
 </script>
 
@@ -185,9 +171,8 @@
 
       <div style="margin-bottom:14px;">
         {% for r in item.recipe %}
-        <span class="recipe-chip recipe-chip-editable" onclick="event.stopPropagation(); fillRecipeForm({{ item.product.id }}, {{ r.material_id }}, {{ r.batch_qty }}, {{ r.yield_count }})">
-          {{ r.batch_qty|round(3) }}{{ r.unit }} {{ r.material_name }} {{ t('makes_label') }} {{ r.yield_count|round(0)|int }} {{ t('loaves_unit') }}
-          <span class="chip-sub">({{ r.quantity_per_unit|round(4) }}{{ r.unit }}{{ t('per_unit_suffix') }})</span>
+        <span class="recipe-chip recipe-chip-editable" onclick="event.stopPropagation(); fillRecipeForm({{ item.product.id }}, {{ r.material_id }}, {{ r.quantity_per_unit }})">
+          {{ r.material_name }} — {{ r.quantity_per_unit|round(4) }}{{ r.unit }} {{ t('per_unit_suffix') }}
           <form method="POST" action="{{ url_for('remove_recipe_ingredient', ingredient_id=r.id) }}" class="inline-form" onclick="event.stopPropagation();"><input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
             <button type="submit" title="{{ t('delete_btn') }}">&times;</button>
           </form>
@@ -205,14 +190,12 @@
             <option value="{{ m.id }}" data-unit="{{ m.unit }}">{{ m.name }}</option>
             {% endfor %}
           </select>
-          <input type="number" step="any" name="batch_qty" id="recipe-batch-{{ item.product.id }}" placeholder="1" required class="recipe-num-input" oninput="updateRecipeRow({{ item.product.id }})">
+          <input type="number" step="any" name="quantity_per_unit" id="recipe-amount-{{ item.product.id }}" placeholder="0.3" required class="recipe-num-input">
           <span class="recipe-unit-label" id="recipe-unit-{{ item.product.id }}">{{ t('unit_choose') }}</span>
-          <span class="recipe-makes-word">{{ t('makes_label') }}</span>
-          <input type="number" step="any" name="yield_count" id="recipe-yield-{{ item.product.id }}" placeholder="20" required class="recipe-num-input" oninput="updateRecipeRow({{ item.product.id }})">
-          <span class="recipe-unit-label">{{ t('loaves_unit') }}</span>
+          <span class="recipe-makes-word">{{ t('per_unit_suffix') }}</span>
           <button type="submit" class="secondary">{{ t('add_to_recipe_btn') }}</button>
         </div>
-        <p class="field-hint recipe-live-preview" id="recipe-preview-{{ item.product.id }}"></p>
+        <p class="field-hint">{{ t('recipe_hint') }}</p>
       </form>
     </div>
     {% endfor %}
diff --git a/translations.py b/translations.py
index 7530428..1dd49d6 100644
--- a/translations.py
+++ b/translations.py
@@ -137,8 +137,8 @@ TR = {
         "selling_price_usd": "តម្លៃលក់ ($)",
         "selling_price_riel": "តម្លៃលក់ (៛)",
         "add_product_btn": "បន្ថែម",
-        "recipe_title": "រូបមន្ត — តើវត្ថុធាតុដើមប៉ុន្មានផលិតបាននំប៉ុន្មានដុំ?",
-        "recipe_hint": "ឧទាហរណ៍៖ ម្សៅ ១គីឡូក្រាម ផលិតបាននំ ២០ដុំ។ ប្រព័ន្ធនឹងគណនាដោយស្វ័យប្រវត្តិថាមួយដុំប្រើប្រាស់ប៉ុន្មាន។",
+        "recipe_title": "រូបមន្ត — តើផលិតផលមួយដុំប្រើប្រាស់វត្ថុធាតុដើមប៉ុន្មាន?",
+        "recipe_hint": "ឧទាហរណ៍៖ អំបិល 0.3 ក្រាម ក្នុងមួយដុំ។ វាយបញ្ចូលចំនួនប្រើប្រាស់សម្រាប់មួយដុំដោយផ្ទាល់។",
         "material_col": "វត្ថុធាតុដើម",
         "batch_qty_label": "បរិមាណប្រើ",
         "yield_count_label": "ផលិតបានប៉ុន្មានដុំ",
@@ -464,8 +464,8 @@ TR = {
         "selling_price_usd": "Selling price ($)",
         "selling_price_riel": "Selling price (៛)",
         "add_product_btn": "Add product",
-        "recipe_title": "Recipe — how much material makes how many breads?",
-        "recipe_hint": "Example: 1kg of flour makes 20 breads. The system works out how much one bread uses automatically.",
+        "recipe_title": "Recipe — how much material does one unit use?",
+        "recipe_hint": "Example: Salt 0.3g per unit. Type the amount one unit uses directly.",
         "material_col": "Material",
         "batch_qty_label": "Amount used",
         "yield_count_label": "Makes how many",
